# 학교알리미 공공데이터 — 미구현 활용 계획

> **이 문서의 목적**: `Explanation1.md`에 기술된 학교알리미 공공데이터 4가지 활용 방식 중,  
> 2026-05-25 현재 **구현되지 않은** 활용 방식 (1)(3)(4)를 별도 기록합니다.  
> 구현 완료된 활용 방식 (2)는 `공공데이터_활용_사업계획서.md` ⑫항에 반영되었습니다.

---

## 구현 현황 요약

| 활용 방식 | 내용 요약 | 구현 상태 |
|----------|----------|----------|
| (1) 학사일정 교차 검증 | KoELECTRA 슬롯 추출 오류를 학교알리미 학사 데이터로 검출 | ❌ 미구현 |
| (2) KcELECTRA 학습 데이터 보강 | 건강·안전/일정/비용 카테고리 564행 증강 | ✅ 학습예정 |
| (3) 학교 공식 정보 인증 | 가정통신문 발신 학교가 학교알리미 등록 학교인지 검증 | ❌ 미구현 |
| (4) 지역별 보급 우선순위 결정 | 학교알리미 학생수 + KESS 다문화 학생수 결합 분석 | ❌ 미구현 |

---

## (1) 학사일정 교차 검증

### 개요

KoELECTRA가 가정통신문에서 추출한 날짜·일정 슬롯(체험학습일·방학식·졸업식·운동회 등)을 학교알리미 공개용데이터의 학사 항목과 교차 검증하여, 슬롯 추출 오류를 자동 검출한다.

### 배경

- KoELECTRA 슬롯 추출 Recall = 0.868 → 11.2%p 미검출 존재
- 학교알리미 학사 데이터에는 전국 학교의 실제 학사 일정(방학식·입학식·졸업식 날짜 등)이 포함되어 있어, 추출된 날짜가 해당 학교의 실제 학사 일정과 일치하는지 검증 가능

### 구현 방향 (미완)

```
1. 학교 식별: 가정통신문 발신 학교명 추출 (NER 또는 헤더 파싱)
2. 학교알리미 API 조회: 해당 학교 학사 일정 데이터 로드
3. 교차 검증: KoELECTRA 추출 날짜 vs 학교알리미 학사 일정 비교
   - 일치: 슬롯 신뢰도 ↑
   - 불일치: 오류 플래그 → 재추출 또는 경고
4. 출력: 검증된 슬롯 + 불일치 건수 통계
```

### 필요 작업

- [ ] 학교알리미 API(data.go.kr) 키 발급 및 학사 데이터 항목 확인
- [ ] 학교명 → 학교 코드 매핑 테이블 구축
- [ ] 교차 검증 로직 구현 (model/ner/ 또는 별도 validation 모듈)
- [ ] KoELECTRA 슬롯 추출 파이프라인과 통합

---

## (3) 학교 공식 정보 인증 (피싱 가정통신문 필터링)

### 개요

가정통신문 발신 학교가 학교알리미에 등록된 실제 학교인지 자동 검증하여, 가짜·피싱 가정통신문을 필터링한다.

### 배경

- 피싱 사례: 위조 학교 명의로 금전 요구, 개인정보 수집 등 악용 가능성
- 학교알리미에는 전국 모든 초·중등학교의 공식 정보(학교명·주소·학교코드 등)가 등록되어 있어 진위 확인 기준으로 활용 가능

### 구현 방향 (미완)

```
1. 발신 학교명 추출: 가정통신문 상단 학교명 파싱
2. 학교알리미 학교 목록과 대조:
   - 등록된 학교: 정상 통신문 → 번역 파이프라인 진행
   - 미등록 학교: 경고 플래그 → 사용자에게 알림
3. 선택적 강화: 학교 주소·연락처까지 대조 (이중 인증)
```

### 필요 작업

- [ ] 전국 학교 목록 데이터셋 구축 (학교알리미 API 또는 파일데이터)
- [ ] 학교명 정규화 로직 (약칭·이전 교명 처리: "OO초" ↔ "OO초등학교")
- [ ] Android 앱 또는 서버 API에 검증 레이어 추가
- [ ] 피싱 의심 케이스 처리 UI (사용자 확인 요청 팝업)

---

## (4) 지역별 보급 우선순위 의사결정 보조

### 개요

학교알리미의 학교 단위 학생수 데이터와 KESS(교육통계서비스) 시군구별 다문화 학생수 데이터를 결합하여 "다문화 학생 비율 높은 학교"를 자동 식별하고, 우선 보급 대상 학교를 선정한다.

### 배경

- 현재 공공데이터 사업계획서에는 시도·시군구 단위 다문화 학생 통계를 활용하고 있으나 (서울·경기·인천 등), **학교 단위** 세분화는 미구현
- 학교알리미는 학교별 학생수를 제공하므로, KESS 다문화 학생수와 결합하면 특정 학교의 다문화 학생 비율을 추정 가능

### 구현 방향 (미완)

```
1. 데이터 수집:
   - 학교알리미: 학교별 전체 학생수 (학교코드 기준)
   - KESS: 시군구별 다문화 학생수 (KESS 교육통계 API)
2. 결합:
   - 시군구 내 각 학교의 다문화 학생 비율 추정 (시군구 비율을 학교별로 적용)
   - 또는 직접 학교알리미 다문화 학생수 항목 활용 (해당 항목 공개 여부 확인 필요)
3. 우선순위 산출:
   - 다문화 학생 비율 상위 N개 학교 자동 추출
   - 지역별·학교급별 분류
4. 활용:
   - B2B 영업 우선 대상 학교 목록 제공
   - 교육청 협력 시 정책 지원 대상 학교 자동 선정
```

### 필요 작업

- [ ] 학교알리미 학교별 학생수 데이터 API 또는 파일 수집
- [ ] KESS API 연동 (시군구별 다문화 학생수)
- [ ] 학교 단위 다문화 학생 비율 추정 모듈 구현
- [ ] 지역별 우선순위 대시보드 또는 보고서 자동 생성 스크립트

---

## 구현 우선순위 제안

| 순위 | 활용 방식 | 이유 |
|------|----------|------|
| 1순위 | (4) 지역별 보급 우선순위 | 공모전 사업성·시장성 항목에 직접 기여, 기존 공공데이터 분석 연장선 |
| 2순위 | (3) 학교 공식 정보 인증 | 서비스 신뢰성·보안 차별점 — 구현 난이도 낮음 (API 호출 + 비교) |
| 3순위 | (1) 학사일정 교차 검증 | KoELECTRA 파이프라인 통합 필요 — 가장 복잡하지만 성능 개선 효과 명확 |

---

*작성일: 2026-05-25 | 담당: 경이 (kyeongyi)*  
*참조: `Explanation1.md` 활용방식 (1)(3)(4) | 구현 완료: `공공데이터_활용_사업계획서.md` ⑫항*
